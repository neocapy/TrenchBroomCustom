[project]
name = "trenchbroom-client"
version = "0.1.0"
description = "Typed blocking Python client for the TrenchBroom HTTP control API"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/trenchbroom_client"]
